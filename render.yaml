# render.yaml
services:
  - type: web
    name: registro-prof
    env: python
    plan: free
    pythonVersion: "3.11"  # ← IMPORTANTE: Python stabile
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn utenti:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: EMAIL_PASSWORD
        sync: false
      - key: SITE_URL
        sync: false
      - key: USE_SQLITE  # ← Forza SQLite per evitare psycopg2
        value: "true"
# ← Niente sezione "databases:" per non creare PostgreSQL
